# Neo4j connection (server-side only — never expose these to the client / NEXT_PUBLIC_)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=change-me

# For Neo4j Aura (managed cloud) the URI looks like:
# NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
